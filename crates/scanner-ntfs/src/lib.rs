//! mathom-scanner-ntfs: raw NTFS MFT scan backend (milestone 5).
//!
//! Windows-only behind the `mft-backend` cargo feature; on other targets the
//! crate compiles to this empty stub so `cargo check` stays green everywhere.
