[package]
name = "mathom-scanner"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam-channel = "0.5.15"
mathom-core = { version = "0.1.0", path = "../core" }
rayon = "1.12.0"

[dev-dependencies]
criterion = "0.8.2"
tempfile = "3"

[[bench]]
name = "scan"
harness = false
