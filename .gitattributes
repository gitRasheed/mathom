# LF in the repo AND in every working tree: native-eol checkouts made the
# Windows CI runner see CRLF and fail prettier --check against LF-tree devs.
* text=auto eol=lf

*.png binary
*.ico binary
*.icns binary
